---
title: "SearchBar.svelte"
description: "Find in sheet(s): a floating bar over the grid (⌘F / ⌘⇧F all sheets)."
source: "src/lib/components/SearchBar.svelte"
generated: true
---

_Auto-generated page showing the full file contents. To replace it with a
hand-written walkthrough, remove `generated: true` from the frontmatter so
`pnpm gen` keeps your edits._

````svelte SearchBar.svelte -n
<script lang="ts">
	// Find in sheet(s): a floating bar over the grid (⌘F / ⌘⇧F all sheets).
	import { app, gotoCell, sheetController, switchSheet } from '../state.svelte';

	let query = $state('');
	let currentMatch = $state(0);
	let inputEl: HTMLInputElement | undefined = $state();

	$effect(() => {
		inputEl?.focus();
		inputEl?.select();
	});

	interface Match {
		sheetIndex: number;
		x: number;
		y: number;
	}

	const matches = $derived.by((): Match[] => {
		void app.redraw;
		const q = query.trim().toLowerCase();
		if (!q) return [];
		const out: Match[] = [];
		const sheetIndexes = app.searchAllSheets
			? sheetController.sheets.map((_, i) => i)
			: [sheetController.currentSheetIndex];
		for (const sheetIndex of sheetIndexes) {
			const found = sheetController.sheets[sheetIndex]
				.getAllCells()
				.filter((c) => c.value.toLowerCase().includes(q))
				.sort((a, b) => a.y - b.y || a.x - b.x)
				.map((c) => ({ sheetIndex, x: c.x, y: c.y }));
			out.push(...found);
		}
		return out;
	});

	// jumping happens from event handlers only — doing it in an effect that
	// also reads reactive state (matches → app.redraw) loops the runtime
	function jumpTo(index: number): void {
		if (matches.length === 0) {
			currentMatch = 0;
			return;
		}
		currentMatch = ((index % matches.length) + matches.length) % matches.length;
		const m = matches[currentMatch];
		if (m.sheetIndex !== sheetController.currentSheetIndex) switchSheet(m.sheetIndex);
		gotoCell(m.x, m.y);
	}

	function step(delta: number): void {
		jumpTo(currentMatch + delta);
	}

	function close(): void {
		app.showSearch = false;
	}

	function onKeyDown(e: KeyboardEvent): void {
		e.stopPropagation();
		if (e.key === 'Escape') close();
		else if (e.key === 'Enter') {
			e.preventDefault();
			step(e.shiftKey ? -1 : 1);
		} else if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'f') {
			e.preventDefault();
			app.searchAllSheets = e.shiftKey;
		}
	}
</script>

<!-- svelte-ignore a11y_no_static_element_interactions -->
<div class="searchbar" onkeydown={onKeyDown}>
	<input
		bind:this={inputEl}
		value={query}
		oninput={(e) => {
			query = e.currentTarget.value;
			jumpTo(0);
		}}
		placeholder="Find in {app.searchAllSheets ? 'all sheets' : 'sheet'}…"
		spellcheck="false"
	/>
	<span class="count" class:none={query !== '' && matches.length === 0}>
		{#if query === ''}&nbsp;{:else if matches.length === 0}0{:else}{currentMatch + 1} of {matches.length}{/if}
	</span>
	<button class="nav" title="Previous (⇧↵)" aria-label="Previous match" onclick={() => step(-1)}>↑</button>
	<button class="nav" title="Next (↵)" aria-label="Next match" onclick={() => step(1)}>↓</button>
	<button
		class="all"
		class:on={app.searchAllSheets}
		title="Search all sheets (⌘⇧F)"
		aria-pressed={app.searchAllSheets}
		onclick={() => {
			app.searchAllSheets = !app.searchAllSheets;
			jumpTo(0);
		}}
	>
		All sheets
	</button>
	<button class="nav close" title="Close (Esc)" aria-label="Close search" onclick={close}>✕</button>
</div>

<style>
	.searchbar {
		position: absolute;
		top: 0.5rem;
		right: 0.75rem;
		z-index: 60;
		display: flex;
		align-items: center;
		gap: 0.35rem;
		background: var(--panel);
		border: 1px solid var(--border);
		border-radius: 6px;
		box-shadow: 0 4px 16px var(--shadow);
		padding: 0.3rem 0.4rem;
		font-size: 0.8rem;
		color: var(--muted);
	}
	input {
		border: none;
		outline: none;
		font: inherit;
		width: 13rem;
		padding: 0.15rem 0.3rem;
	}
	.count {
		min-width: 3.6rem;
		text-align: right;
		font-variant-numeric: tabular-nums;
		color: var(--faint);
		font-size: 0.72rem;
	}
	.count.none {
		color: var(--error);
	}
	.nav {
		background: none;
		border: none;
		font: inherit;
		color: var(--muted);
		cursor: pointer;
		padding: 0.15rem 0.35rem;
		border-radius: 4px;
	}
	.nav:hover {
		background: var(--hover);
	}
	.all {
		background: none;
		border: 1px solid var(--border);
		border-radius: 9px;
		font: inherit;
		font-size: 0.68rem;
		color: var(--faint);
		cursor: pointer;
		padding: 0.1rem 0.5rem;
		white-space: nowrap;
	}
	.all.on {
		color: var(--accent);
		border-color: var(--accent);
		background: var(--accent-soft);
	}
	.close {
		color: var(--faint);
	}
</style>
````
