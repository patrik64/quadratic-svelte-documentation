---
title: "mathematics.rs"
description: "Rust module mathematics.rs of the quadratic-core formula engine."
source: "quadratic-core/src/formulas/functions/mathematics.rs"
generated: true
---

_Auto-generated page showing the full file contents. To replace it with a
hand-written walkthrough, remove `generated: true` from the frontmatter so
`pnpm gen` keeps your edits._

````rust mathematics.rs -n
use super::*;

pub const CATEGORY: FormulaFunctionCategory = FormulaFunctionCategory {
    include_in_docs: true,
    include_in_completions: true,
    name: "Mathematics functions",
    docs: "",
    get_functions,
};

#[derive(Copy, Clone)]
enum RoundMode {
    Nearest,
    AwayFromZero,
    TowardZero,
}

/// Shared implementation for ROUND/ROUNDUP/ROUNDDOWN: 1 or 2 arguments.
fn round_fn(args: Spanned<Vec<Spanned<Value>>>, mode: RoundMode) -> FormulaResult {
    let span = args.span;
    let mut a = args.inner.into_iter();
    let (Some(number), digits) = (a.next(), a.next()) else {
        return Err(FormulaErrorMsg::BadArgumentCount.with_span(span));
    };
    if a.next().is_some() {
        return Err(FormulaErrorMsg::BadArgumentCount.with_span(span));
    }
    let digits = match digits {
        Some(d) => d.to_integer()?,
        None => 0,
    };
    let factor = 10.0_f64.powi(digits as i32);
    let x = number.to_number()? * factor;
    let rounded = match mode {
        RoundMode::Nearest => x.round(),
        RoundMode::AwayFromZero => {
            if x >= 0.0 {
                x.ceil()
            } else {
                x.floor()
            }
        }
        RoundMode::TowardZero => x.trunc(),
    };
    Ok(Value::Number(rounded / factor))
}

/// Shared implementation for CEILING/FLOOR: `(number)` or `(number, increment)`.
/// Matches the modern core: a zero increment yields 0.
fn increment_round(args: Spanned<Vec<Spanned<Value>>>, round: fn(f64) -> f64) -> FormulaResult {
    let span = args.span;
    let mut a = args.inner.into_iter();
    let (Some(number), increment) = (a.next(), a.next()) else {
        return Err(FormulaErrorMsg::BadArgumentCount.with_span(span));
    };
    if a.next().is_some() {
        return Err(FormulaErrorMsg::BadArgumentCount.with_span(span));
    }
    let increment = match increment {
        Some(i) => i.to_number()?,
        None => 1.0,
    };
    if increment == 0.0 {
        return Ok(Value::Number(0.0));
    }
    let n = number.to_number()?;
    Ok(Value::Number(round(n / increment) * increment))
}

fn get_functions() -> Vec<FormulaFunction> {
    vec![
        FormulaFunction {
            name: "SUM",
            arg_completion: "${1:a, b, ...}",
            usages: &["a, b, ..."],
            examples: &["SUM(B2:C6, 15, E1)"],
            doc: "Adds all values.\nReturns `0` if given no values.",
            eval: util::pure_fn(|args| Ok(Value::Number(util::sum(&args.inner)))),
        },
        FormulaFunction {
            name: "PRODUCT",
            arg_completion: "${1:a, b, ...}",
            usages: &["a, b, ..."],
            examples: &["PRODUCT(B2:C6, 0.002, E1)"],
            doc: "Multiplies all values.\nReturns 1 if given no values.",
            eval: util::pure_fn(|args| Ok(Value::Number(util::product(&args.inner)))),
        },
        FormulaFunction {
            name: "ABS",
            arg_completion: "${1:number}",
            usages: &["number"],
            examples: &["ABS(-4)"],
            doc: "Returns the absolute value of a number.",
            eval: util::array_mapped(|[number]| Ok(Value::Number(number.to_number()?.abs()))),
        },
        FormulaFunction {
            name: "SQRT",
            arg_completion: "${1:number}",
            usages: &["number"],
            examples: &["SQRT(2)"],
            doc: "Returns the square root of a number.",
            eval: util::array_mapped(|[number]| Ok(Value::Number(number.to_number()?.sqrt()))),
        },
        FormulaFunction {
            name: "POWER",
            arg_completion: "${1:base}, ${2:exponent}",
            usages: &["base, exponent"],
            examples: &["POWER(2, 10)"],
            doc: "Returns `base` raised to the power of `exponent`.",
            eval: util::array_mapped(|[base, exponent]| {
                Ok(Value::Number(base.to_number()?.powf(exponent.to_number()?)))
            }),
        },
        FormulaFunction {
            name: "EXP",
            arg_completion: "${1:number}",
            usages: &["number"],
            examples: &["EXP(1)"],
            doc: "Returns e raised to the power of `number`.",
            eval: util::array_mapped(|[number]| Ok(Value::Number(number.to_number()?.exp()))),
        },
        FormulaFunction {
            name: "LN",
            arg_completion: "${1:number}",
            usages: &["number"],
            examples: &["LN(50)"],
            doc: "Returns the natural logarithm of a number.",
            eval: util::array_mapped(|[number]| Ok(Value::Number(number.to_number()?.ln()))),
        },
        FormulaFunction {
            name: "LOG",
            arg_completion: "${1:number}",
            usages: &["number", "number, base"],
            examples: &["LOG(100)", "LOG(8, 2)"],
            doc: "Returns the logarithm of a number. Uses base 10 unless a base is given.",
            eval: util::pure_fn(|args| {
                let span = args.span;
                let mut a = args.inner.into_iter();
                let (Some(number), base) = (a.next(), a.next()) else {
                    return Err(FormulaErrorMsg::BadArgumentCount.with_span(span));
                };
                if a.next().is_some() {
                    return Err(FormulaErrorMsg::BadArgumentCount.with_span(span));
                }
                let base = match base {
                    Some(b) => b.to_number()?,
                    None => 10.0,
                };
                if base <= 0.0 {
                    return Err(FormulaErrorMsg::BadNumber.with_span(span));
                }
                Ok(Value::Number(number.to_number()?.log(base)))
            }),
        },
        FormulaFunction {
            name: "LOG10",
            arg_completion: "${1:number}",
            usages: &["number"],
            examples: &["LOG10(1000)"],
            doc: "Returns the base-10 logarithm of a number.",
            eval: util::array_mapped(|[number]| Ok(Value::Number(number.to_number()?.log10()))),
        },
        FormulaFunction {
            name: "MOD",
            arg_completion: "${1:number}, ${2:divisor}",
            usages: &["number, divisor"],
            examples: &["MOD(7, 3)", "MOD(-7, 3)"],
            doc: "Returns the remainder after dividing `number` by `divisor`. \
                  The result has the same sign as `divisor`.",
            eval: util::array_mapped(|[number, divisor]| {
                let d = divisor.to_number()?;
                if d == 0.0 {
                    return Err(FormulaErrorMsg::DivideByZero.with_span(divisor.span));
                }
                let n = number.to_number()?;
                Ok(Value::Number(((n % d) + d) % d))
            }),
        },
        FormulaFunction {
            name: "ROUND",
            arg_completion: "${1:number}",
            usages: &["number", "number, digits"],
            examples: &["ROUND(3.14159)", "ROUND(3.14159, 2)"],
            doc: "Rounds a number to the given number of digits after the decimal point \
                  (default 0).",
            eval: util::pure_fn(|args| round_fn(args, RoundMode::Nearest)),
        },
        FormulaFunction {
            name: "ROUNDUP",
            arg_completion: "${1:number}",
            usages: &["number", "number, digits"],
            examples: &["ROUNDUP(3.14159, 2)"],
            doc: "Rounds a number away from zero to the given number of digits after the \
                  decimal point (default 0).",
            eval: util::pure_fn(|args| round_fn(args, RoundMode::AwayFromZero)),
        },
        FormulaFunction {
            name: "ROUNDDOWN",
            arg_completion: "${1:number}",
            usages: &["number", "number, digits"],
            examples: &["ROUNDDOWN(3.14159, 2)"],
            doc: "Rounds a number toward zero to the given number of digits after the \
                  decimal point (default 0).",
            eval: util::pure_fn(|args| round_fn(args, RoundMode::TowardZero)),
        },
        FormulaFunction {
            name: "INT",
            arg_completion: "${1:number}",
            usages: &["number"],
            examples: &["INT(8.9)", "INT(-8.9)"],
            doc: "Rounds a number down to the nearest integer.",
            eval: util::array_mapped(|[number]| Ok(Value::Number(number.to_number()?.floor()))),
        },
        FormulaFunction {
            name: "CEILING",
            arg_completion: "${1:number}",
            usages: &["number", "number, increment"],
            examples: &["CEILING(8.1)", "CEILING(6.5, 2)"],
            doc: "Rounds a number up to the next multiple of `increment` (default 1).",
            eval: util::pure_fn(|args| increment_round(args, f64::ceil)),
        },
        FormulaFunction {
            name: "FLOOR",
            arg_completion: "${1:number}",
            usages: &["number", "number, increment"],
            examples: &["FLOOR(8.9)", "FLOOR(6.5, 2)"],
            doc: "Rounds a number down to the previous multiple of `increment` (default 1).",
            eval: util::pure_fn(|args| increment_round(args, f64::floor)),
        },
        // Constants
        FormulaFunction {
            name: "PI",
            arg_completion: "",
            usages: &[""],
            examples: &["PI()"],
            doc: "Returns π, the circle constant.",
            eval: util::constant_fn(Value::Number(std::f64::consts::PI)),
        },
        FormulaFunction {
            name: "TAU",
            arg_completion: "",
            usages: &[""],
            examples: &["TAU()"],
            doc: "Returns τ, the circle constant equal to 2π.",
            eval: util::constant_fn(Value::Number(std::f64::consts::TAU)),
        },
    ]
}

#[cfg(test)]
mod tests {
    use crate::formulas::tests::*;

    #[test]
    fn test_sum() {
        let g = &mut NoGrid;
        assert_eq!("0", eval_to_string(g, "SUM(\"\", \"abc\")"));
        assert_eq!("12", eval_to_string(g, "SUM(\"\", \"abc\", 12)"));
        assert_eq!("27", eval_to_string(g, "SUM(0..5, \"\", \"abc\", 12)"));
    }

    #[test]
    fn test_product() {
        let g = &mut NoGrid;
        assert_eq!("1", eval_to_string(g, "PRODUCT(\"\", \"abc\")"));
        assert_eq!("12", eval_to_string(g, "PRODUCT(\"\", \"abc\", 12)"));
        assert_eq!(
            "1440",
            eval_to_string(g, "PRODUCT(1..5, \"\", \"abc\", 12)")
        );
        assert_eq!("0", eval_to_string(g, "PRODUCT(0..5, \"\", \"abc\", 12)"));
    }

    #[test]
    fn test_formula_abs() {
        let g = &mut NoGrid;
        assert_eq!("10", eval_to_string(g, "ABS(-10)"));
        assert_eq!("10", eval_to_string(g, "ABS(10)"));
        assert_eq!(
            FormulaErrorMsg::BadArgumentCount,
            parse_formula("ABS()", Pos::ORIGIN)
                .unwrap()
                .eval_blocking(g, Pos::ORIGIN)
                .unwrap_err()
                .msg,
        );
        assert_eq!(
            FormulaErrorMsg::BadArgumentCount,
            parse_formula("ABS(16, 17)", Pos::ORIGIN)
                .unwrap()
                .eval_blocking(g, Pos::ORIGIN)
                .unwrap_err()
                .msg,
        );
    }

    #[test]
    fn test_formula_sqrt() {
        let g = &mut NoGrid;
        crate::util::assert_f64_approx_eq(3.0_f64.sqrt(), &eval_to_string(g, "SQRT(3)"));
        assert_eq!("4", eval_to_string(g, "SQRT(16)"));
        assert_eq!(
            FormulaErrorMsg::BadArgumentCount,
            parse_formula("SQRT()", Pos::ORIGIN)
                .unwrap()
                .eval_blocking(g, Pos::ORIGIN)
                .unwrap_err()
                .msg,
        );
        assert_eq!(
            FormulaErrorMsg::BadArgumentCount,
            parse_formula("SQRT(16, 17)", Pos::ORIGIN)
                .unwrap()
                .eval_blocking(g, Pos::ORIGIN)
                .unwrap_err()
                .msg,
        );
    }

    #[test]
    fn test_formula_pi() {
        let g = &mut NoGrid;
        assert!(eval_to_string(g, "PI()").starts_with("3.14159"));
        assert_eq!(
            FormulaErrorMsg::BadArgumentCount,
            parse_formula("PI(16)", Pos::ORIGIN)
                .unwrap()
                .eval_blocking(g, Pos::ORIGIN)
                .unwrap_err()
                .msg,
        );
    }

    #[test]
    fn test_formula_tau() {
        let g = &mut NoGrid;
        assert!(eval_to_string(g, "TAU()").starts_with("6.283"));
        assert_eq!(
            FormulaErrorMsg::BadArgumentCount,
            parse_formula("TAU(16)", Pos::ORIGIN)
                .unwrap()
                .eval_blocking(g, Pos::ORIGIN)
                .unwrap_err()
                .msg,
        );
    }
}
````
